# macOS packaging notes

## Homebrew

See `homebrew/netforge.rb` stub. Publish a tagged release tarball, fill `sha256`, host a personal tap or submit to a community tap.

## Manual

`install-network-auto.sh` remains the supported path (LaunchDaemon).

No telemetry; brew only distributes files.
